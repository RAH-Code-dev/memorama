
export default function useSubRoomLogic() {
  // as the players come along with the subrooms
  // we'll receive the players here and 
  // sort them into the subrooms

  return {
    Nothing_You_Could_Do
  }
}
